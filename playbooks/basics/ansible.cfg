[defaults]
inventory = ../../inventories/inventory
remote_user = devops
host_key_checking = false

[privilege_escalation] 
become = True
become_method = sudo
become_user = root
become_ask_pass = False
